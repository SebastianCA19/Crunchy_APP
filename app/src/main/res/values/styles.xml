<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Estilo personalizado para el Dialog que aparece desde la derecha -->
    <style name="CartDialogRight" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowMinWidthMajor">40%</item>
        <item name="android:windowAnimationStyle">@style/CartDialogAnimation</item>
    </style>

    <!-- Animaciones para entrada/salida lateral -->
    <style name="CartDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_in_right</item>
        <item name="android:windowExitAnimation">@anim/slide_out_right</item>
    </style>

</resources>
